name: Build and Setup

description: Build and Setup

runs:
  using: composite
  steps:
    - name: Setup Repo
      uses: ./.github/actions/ci/setup

    - name: Build Code
      shell: bash
      run: go build -v ./...

    - name: Run Tests
      shell: bash
      run: go test -v ./...
